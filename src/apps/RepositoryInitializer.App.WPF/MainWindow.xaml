<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RepositoryInitializer.App.WPF"
        x:Class="RepositoryInitializer.App.WPF.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Variables}" />
        <Label Grid.Row="1" Content="Path:" Width="150" HorizontalAlignment="Left" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Text="{Binding Path}" Margin="150,5,5,5" />
        <Button Grid.Row="2" Content="Replace" Click="Button_Click" />
    </Grid>
</Window>
